sudo: false
dist: trusty
language: python
python:
 - "2.7"
env:
 - PROF=deb9
addons:
  apt:
    packages:
    - openssl
    - default-jdk-headless
install:
  - pip install -r requirements-${PROF}.txt
script:
  - ./test.sh
